export enum UrlsEnum {
  baseURL = 'http://localhost:3000',
  signUp = 'auth/signup',
  logIn = 'auth/signin',
  users = 'users',
  boards = 'boards',
  boardsSet = 'boardsSet',
  columns = 'columns',
  tasks = 'tasks',
  file = 'file',
}
